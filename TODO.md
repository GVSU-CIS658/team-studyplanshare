# TODO

- Add course name search/filter on the home page (backend already supports `?courseName=`)
- Add sort toggle on home page (recent vs popular — backend supports `?sortBy=popular`)
- Firebase clean up (sent to Ryley)